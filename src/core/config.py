"""
Application Configuration & Settings.

Defines the Settings dataclass for runtime defaults, including OCIO paths,
Delta E thresholds, and UI display preferences.
Generates a singleton instance for global access across the tool.
"""